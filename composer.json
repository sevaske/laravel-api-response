{
    "name": "sevaske/laravel-api-response",
    "description": "The package for building clean, consistent, and predictable JSON API responses in laravel applications.",
    "type": "library",
    "license": "MIT",
    "keywords": [
        "laravel",
        "api",
        "response",
        "json",
        "api response",
        "laravel api",
        "http response"
    ],
    "authors": [
        {
            "name": "sevaske"
        }
    ],
    "autoload": {
        "psr-4": {
            "Sevaske\\LaravelApiResponse\\": "src/"
        },
        "files": [
            "src/helpers.php"
        ]
    },
    "autoload-dev": {
        "psr-4": {
            "Tests\\": "tests/"
        },
        "files": [
            "ide-helper/response-factory.php"
        ]
    },
    "scripts": {
        "test": "vendor/bin/phpunit",
        "analyse": "vendor/bin/phpstan analyse",
        "lint": "vendor/bin/pint",
        "check": [
            "@lint",
            "@analyse",
            "@test"
        ],
        "post-autoload-dump": [
            "@clear",
            "@prepare"
        ],
        "clear": "@php vendor/bin/testbench package:purge-skeleton --ansi",
        "prepare": "@php vendor/bin/testbench package:discover --ansi",
        "build": "@php vendor/bin/testbench workbench:build --ansi",
        "serve": [
            "Composer\\Config::disableProcessTimeout",
            "@build",
            "@php vendor/bin/testbench serve --ansi"
        ]
    },
    "require": {
        "php": "^8.3",
        "illuminate/contracts": "^11.0|^12.0|^13.0",
        "illuminate/support": "^11.0|^12.0|^13.0",
        "sevaske/api-response-payload": "^1.0"
    },
    "require-dev": {
        "laravel/pint": "^1.0",
        "phpunit/phpunit": "^10.0|^11.0|^12.0",
        "orchestra/testbench": "^9.0|^10.0|^11.0",
        "larastan/larastan": "^3.10"
    },
    "extra": {
        "laravel": {
            "providers": [
                "Sevaske\\LaravelApiResponse\\Providers\\ApiResponseServiceProvider"
            ],
            "aliases": {
                "ApiResponse": "Sevaske\\LaravelApiResponse\\Facades\\ApiResponse"
            }
        }
    },
    "prefer-stable": true
}
